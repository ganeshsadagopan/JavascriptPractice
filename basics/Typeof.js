let value=100
console.log(typeof(value))
// This is a simple JavaScript program that declares a variable 'value' and assigns it the value of 100, then prints the type of 'value' to the console using the 'typeof' operator. The expected output will be "number" since 100 is a numeric value.

let text
console.log(typeof(text))

let isAvailable=null
console.log(typeof(isAvailable))
// This is a simple JavaScript program that declares a variable 'isAvailable' and assigns it the value of null, then prints the type of 'isAvailable' to the console using the 'typeof' operator. The expected output will be "object" since in JavaScript, null is considered an object type.
